// Nunjucks globals are registered in nunjucks.js. The govukRebrand flag was
// removed in govuk-frontend 6, where the refreshed GOV.UK branding is the
// default, so there are no globals to register at the moment.
export {}
